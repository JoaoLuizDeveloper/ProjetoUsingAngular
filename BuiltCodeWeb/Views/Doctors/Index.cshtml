<div class="p-4" style="border-radius:20px;">
    <div class="row">
        <div class="col-6">
            <h2 class="text-primary">Listagem de Patrimonios</h2>
        </div>
        <div class="col-6 text-right">
            <a href="@Url.Action("Upsert", "Doctors")" class="btn btn-primary text-white"><i class="fas fa-plus"></i> &nbsp; Create New Doctor</a>
        </div>
    </div>
    <br />
    <br />
    <div style="background-color:#cde8e2" class="p-3 rounded">
        @*<table id="tblData" class="table table-striped table-bordered" style="width:100%">
            <thead>
                <tr class="table-danger">
                    <th>Marca</th>
                    <th>Nome</th>
                    <th>N° Tombamento</th>
                    <th>Descrição</th>
                    <th></th>
                </tr>
            </thead>
        </table>*@

        <div data-ng-controller="doctorController">
            <div class="panel panel-default">
                <div class="panel-heading">Lista de Doctors</div>
                <div class="panel-body">
                    <div class="row">
                        <div class="col-md-12">
                            <strong>{{erro}}</strong>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-12">
                            <div class="table-responsive">
                                <table class="table table-bordered table-striped table-hover" style="width:100%">
                                    <tr>
                                        <td>Id</td>
                                        <td>Name</td>
                                        <td>CRM</td>
                                        <td>CRMUF</td>
                                        <td>Date</td>
                                    </tr>
                                    <tr data-ng-repeat="doctor in listadoctors">
                                        <td>{{doctor.Id}}</td>
                                        <td>{{doctor.Name}}</td>
                                        <td>{{doctor.CRM}}</td>
                                        <td>{{doctor.CRMUF}}</td>
                                        <td>{{doctor.DateCreated}}</td>
                                    </tr>
                                </table>
                            </div>                            
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@*@section Scripts{
    <script src="~/js/patrimonio.js"></script>
}*@